# Trip Curator — MVP

Small demo project: chat UI + RAG pipeline to generate personalized travel itineraries.

## Goals (v0.1)
- Chat frontend (React)
- Backend (FastAPI)
- RAG: ingest docs → vector DB (Chroma) → retrieval
- Run locally with Docker Compose

## Day 1
- Repo scaffold, git setup, basic folders
- Add simple FastAPI + React placeholders next

## Run (dev)
TBD — will be filled day 2 after scaffold files.

